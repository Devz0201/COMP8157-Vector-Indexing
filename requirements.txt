# Core (required) -- all available as prebuilt wheels, no compilation needed.
faiss-cpu>=1.8.0
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
PyYAML>=6.0

# Optional -- only needed for the 'sbert' embedding backend (real Sentence-BERT).
# Install separately:  pip install sentence-transformers
# sentence-transformers>=2.2
